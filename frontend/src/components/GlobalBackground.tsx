const GlobalBackground = () => {
  return (
    <div className="fixed inset-0 w-full h-full z-[-1] bg-gradient-to-br from-red-50 via-white to-orange-50"></div>
  );
};

export default GlobalBackground;
