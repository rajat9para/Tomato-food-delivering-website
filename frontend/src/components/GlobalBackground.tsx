const GlobalBackground = () => {
  return (
    <div className="fixed inset-0 w-full h-full z-[-1] bg-gradient-to-br from-pink-50 via-white to-purple-50"></div>
  );
};

export default GlobalBackground;
